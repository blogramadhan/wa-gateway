# WhatsApp Gateway Environment Configuration
# For Docker: Copy this file to docker.env
# For Local: Copy this file to .env

# Server Configuration
PORT=5000
NODE_ENV=production

# WhatsApp Configuration
SESSION_NAME=wa-gateway-session
WEBHOOK_URL=http://localhost:5000/webhook

# Debug Configuration (set to true to enable console QR code and detailed logging)
DEBUG=false

# Docker specific settings
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable

# Optional: Database URL (if using external database)
# DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Redis URL (if using Redis for session storage)
# REDIS_URL=redis://localhost:6379

# Optional: External webhook URL
# EXTERNAL_WEBHOOK_URL=https://your-webhook-endpoint.com/webhook

# Optional: API Key for authentication (if needed)
# API_KEY=your-secret-api-key

# Optional: Rate limiting settings
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100
